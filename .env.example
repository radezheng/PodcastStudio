# PodcastStudio environment
# Copy to .env in THIS folder.

# Deployment preset (used by `azd provision` via infra/main.bicep)
# - t4  : Consumption-GPU-NC8as-T4 + vibevoice/VibeVoice-1.5B (default)
# - a100: Consumption-GPU-NC24-A100 + vibevoice/VibeVoice-7B (recommended for Chinese/best quality)
# GPU_SKU=t4

# Azure OpenAI (required)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT_NAME=

# Optional (advanced): explicitly set the OpenAI v1 compatibility base URL.
# If omitted, the backend uses `${AZURE_OPENAI_ENDPOINT}/openai/v1/`.
# Example:
# AZURE_OPENAI_BASE_URL=https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/

# Backend -> worker URL
TTS_WORKER_URL=http://localhost:8002

# Shared persistent storage root (DB/config/output). Recommended to mount this to Azure Storage in containers.
# If relative, it's resolved relative to the project root.
PODCASTSTUDIO_STORAGE_DIR=storage

# Azure Container Apps + Azure Files recommended setting (matches `infra/main.bicep` mountPath)
# PODCASTSTUDIO_STORAGE_DIR=/mnt/storage

# VibeVoice worker (optional)
# Model override (optional). If omitted, deployments derive this from GPU_SKU.
VIBEVOICE_MODEL_ID=vibevoice/VibeVoice-1.5B
VIBEVOICE_SPEAKER_NAMES="Xinran Anchen"
# Optional override:
# VIBEVOICE_OUTPUT_DIR=/absolute/path/to/storage/outputs/tts_worker
